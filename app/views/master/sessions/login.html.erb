<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Agent Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex items-center justify-center font-sans bg-gray-100">
  <div class="flex w-[900px] max-w-[95vw] bg-white rounded-2xl shadow-2xl overflow-hidden">
    
    <!-- Left Side -->
    <div class="hidden md:flex flex-col justify-center items-center bg-indigo-500 w-1/2 p-10">
      <div class="text-3xl font-bold text-white mb-4">Welcome!</div>
      <div class="text-lg text-indigo-100 mb-8 text-center">
        Access your dashboard, manage customers, and help them with their financial needs.
      </div>
      <img
        src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png"
        alt="Agent Illustration"
        class="w-48 h-48 object-contain"
      />
    </div>

    <!-- Right Side -->
    <div class="flex-1 flex flex-col justify-center p-10">
      <div class="text-center text-3xl font-semibold text-indigo-500 mb-2">
        Master Login
      </div>

      <!-- Tabs -->
      <div class="flex mb-8 mt-4">
        <button
          class="w-100 flex-1 py-3 rounded-l-lg font-medium transition text-lg bg-indigo-500 text-white shadow"
        >
          Username & Password
        </button>
        <button
          class="flex-1 py-3 rounded-r-lg font-medium transition text-lg bg-gray-100 text-gray-700 hover:bg-indigo-100"
        >
          Mobile Number
        </button>
      </div>

      <!-- Username & Password Login -->
    <%= form_with url: master_sessions_create_path, method: :post do |f| %>
        <div class="text-center mb-4">
            <% if flash[:alert] %>
              <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg">
                <%= flash[:alert] %>
              </div>
            <% elsif flash[:notice] %>
              <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg">
                <%= flash[:notice] %>
              </div>
            <% end %>
        </div>
        <div>
          <label class="block text-gray-700 font-medium mb-1">Username</label>
          <input
            name="email"
            type="text"
            class="w-full px-4 py-3 border border-gray-200 rounded-lg bg-gray-50 text-base outline-none focus:border-indigo-500 transition"
            placeholder="Enter your username"
          />
        </div>
        <div class="mt-3">
          <label class="block text-gray-700 font-medium mb-1">Password</label>
          <input
            name="password"
            type="password"
            class="w-full px-4 py-3 border border-gray-200 rounded-lg bg-gray-50 text-base outline-none focus:border-indigo-500 transition"
            placeholder="Enter your password"
          />
        </div>
        <div class="flex justify-end items-center mt-3 mb-3">
          <a href="forgot-password.html" class="cursor-pointer text-indigo-600 hover:underline text-sm font-medium">
            Forgot Password?
          </a>
        </div>
        <button
          type="submit"
          class="cursor-pointer w-full bg-indigo-500 hover:bg-indigo-600 text-white font-semibold rounded-lg py-3 mt-2 transition text-lg"
        >
          Login
        </button>
       <%end%>

      <!-- Error message example -->
      <div class="text-red-500 mt-3 hidden">Invalid username or password</div>

    </div>
  </div>
</body>
</html>