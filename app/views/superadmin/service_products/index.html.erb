<div class="mt-6 px-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-bold">Service Product List</h1>
    <a href="<%=new_superadmin_service_product_path(category_id: @cat_id)%>"
       class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition">
      Add Service Product
    </a>
  </div>

  <!-- Table Container -->
  <div class="overflow-x-auto bg-white shadow-md rounded-lg">
    <table class="w-full text-sm text-left text-gray-600">
      <thead class="text-xs uppercase bg-gray-100 text-gray-700">
        <tr>
          <th scope="col" class="px-6 py-3">ID</th>
          <th scope="col" class="px-6 py-3">Category Name</th>
          <th scope="col" class="px-6 py-3">Category</th>
          <th scope="col" class="px-6 py-3 text-center">Actions</th>
        </tr>
      </thead>
      <tbody>
        <%@service_products.each do |procuts|%>
        <tr class="bg-white border-b hover:bg-gray-50">
          <td class="px-6 py-4 font-medium text-gray-900"><%=procuts.id%></td>
          <td class="px-6 py-4"><%=procuts.company_name%></td>
          <td class="px-6 py-4"><%#=procuts.category%></td>
          <td class="flex px-6 py-4 justify-center gap-2">
            <a href="<%= view_product_item_superadmin_service_product_path(id: procuts.id) %>" 
                 class="px-3 py-1 text-sm font-medium text-white bg-green-500 rounded-lg hover:bg-green-600 transition">
                View Service Product items
              </a>
            <a href="<%= edit_superadmin_service_product_path(id: procuts, service_id: @service_id) %>" 
                 class="px-3 py-1 text-sm font-medium text-white bg-yellow-500 rounded-lg hover:bg-yellow-600 transition">
                Edit
              </a>
          <%= button_to "Delete", superadmin_service_products_path(id: procuts, service_id: @service_id), 
              method: :delete, 
              data: { turbo_confirm: "Are you sure you want to delete this category?" }, 
              class: "px-3 py-1 text-sm font-medium text-white bg-red-500 rounded-lg hover:bg-red-600 transition" %>

          </td>
        </tr>
        <%end%>
      </tbody>
    </table>
  </div>
</div>
