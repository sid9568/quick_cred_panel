<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">

  <div class="bg-white rounded-2xl shadow-xl w-full max-w-md p-6 sm:p-8">
    <h2 class="text-2xl sm:text-3xl font-semibold text-center text-gray-800 mb-6">
      🔑 Forgot Password
    </h2>

    <!-- Forgot Password Form -->
    <%= form_with url: superadmin_sessions_forgot_email_path, method: :post, local: true do |f| %>
      <div id="step1" class="space-y-4">
        <%= f.email_field :email,
          required: true,
          placeholder: "Enter your email",
          class: "w-full border rounded-lg px-4 py-2"
        %>

        <%= f.submit "Send OTP",
          class: "w-full bg-blue-600 text-white py-2 rounded-lg hover:bg-blue-700 transition"
        %>
      </div>
    <% end %>

    <p class="text-center text-sm text-gray-500 mt-4">
      Go Back?
      <a href="/login" class="text-blue-600 hover:underline">Login</a>
    </p>
  </div>
</body>
</html>
